<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="PowerDock.DockWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cpcontrols="using:Microsoft.CmdPal.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:help="using:Microsoft.CmdPal.UI.Helpers"
    xmlns:local="using:PowerDock"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    Title="PowerDock"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ScrollViewer
            HorizontalAlignment="Stretch"
            HorizontalScrollBarVisibility="Hidden"
            HorizontalScrollMode="Enabled"
            VerticalScrollBarVisibility="Auto"
            VerticalScrollMode="Disabled">

            <!--<Button Padding="2" Tapped="Button_Tapped">
                <FontIcon FontSize="10" Glyph="&#xE149;" />
            </Button>-->
            <!--<ListView Grid.Row="1" ItemsSource="{x:Bind TaskbarItems, Mode=OneWay}">

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:TaskbarApp">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <cpcontrols:IconBox
                            x:Name="IconBorder"
                            Width="16"
                            Height="16"
                            VerticalAlignment="Center"
                            AutomationProperties.AccessibilityView="Raw"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            SourceKey="{x:Bind Icon, Mode=OneWay}"
                            SourceRequested="{x:Bind help:IconCacheProvider.SourceRequested}" />
                        <TextBlock
                            MaxWidth="200"
                            Text="{x:Bind Title, Mode=OneWay}"
                            TextTrimming="WordEllipsis" />
                    </StackPanel>
                </DataTemplate>

            </ListView.ItemTemplate>
        </ListView>-->
            <ItemsRepeater HorizontalAlignment="Left" ItemsSource="{x:Bind ViewModel.Apps, Mode=OneWay}">
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="local:TaskbarApp">
                        <Button
                            Padding="2"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            Command="{x:Bind SwitchToCommand, Mode=OneWay}"
                            ToolTipService.ToolTip="{x:Bind Title, Mode=OneWay}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <cpcontrols:IconBox
                                    x:Name="IconBorder"
                                    Width="16"
                                    Height="16"
                                    VerticalAlignment="Center"
                                    AutomationProperties.AccessibilityView="Raw"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    SourceKey="{x:Bind Icon, Mode=OneWay}"
                                    SourceRequested="{x:Bind help:IconCacheProvider.SourceRequested}" />
                                <TextBlock
                                    MaxWidth="100"
                                    FontSize="10"
                                    Text="{x:Bind Title, Mode=OneWay}"
                                    TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </Button>
                    </DataTemplate>

                </ItemsRepeater.ItemTemplate>
                <!--<ItemsRepeater.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" Spacing="4" />
                    </ItemsPanelTemplate>
                </ItemsRepeater.ItemsPanel>-->
                <ItemsRepeater.Layout>
                    <StackLayout Orientation="Horizontal" Spacing="4" />
                </ItemsRepeater.Layout>
                <ItemsRepeater.Transitions>
                    <TransitionCollection />

                </ItemsRepeater.Transitions>
            </ItemsRepeater>

        </ScrollViewer>
    </Grid>
</Window>
